services:
  - type: web
    name: arcadecraft-engine
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: ADMIN_PIN
        sync: false
